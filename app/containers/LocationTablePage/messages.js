/*
 * LocationTablePage Messages
 *
 * This contains all the text for the LocationTablePage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LocationTablePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Locations',
  },
});
